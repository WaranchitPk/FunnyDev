import React from 'react';
import { Typography } from '@material-ui/core';

const TitleCourseMain = () => (
  <Typography variant='title' align="center">
    จำนวนรายวิชาของทุกเขตพื้นที่
  </Typography>
);

export default TitleCourseMain;
