import React from "react";
import {Typography} from '@material-ui/core';
const TitleTeacher = () => (
  <Typography variant="title" align="center">
    Teacher
  </Typography>
);

export default TitleTeacher;
